pipeline {
    agent {
        label 'macos'
    }

    stages {
        stage('Stage 1') {
            steps {
                sh 'ls -la'
            }
        }

        stage('Stage 2') {
            steps {
                sh 'ls -la'
            }
        }
    }
}
